# Shared Contracts

This folder stores cross-service contracts for SynapseRoute.

## Current Artifacts

- `order.schema.json`: baseline order payload contract.
- `optimize-route.schema.json`: baseline request/response contract for route optimization.

## Notes

These schemas are intentionally minimal and should evolve with endpoint hardening.
